{
  "name": "watch_reapp",
  "version": "1.0.3",
  "description": "watches all files and folders restarts app when changes are made, crashes on error and waits for change to restart, very simple app ",
  "main": "watch_reapp.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "watch",
    "watcher",
    "auto-restart"
  ],
    "repository": {
    "type": "git",
    "url": "watch_reapp"
  },
  "author": "mattias lemmens",
  "license": "ISC",
  "dependencies": {
    "on-file-change": "^0.1.0"
  }
}
